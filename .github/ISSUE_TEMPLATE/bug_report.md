---
name: Bug report
about: Something the picker does that doesn't match the README's promise
labels: bug
---

<!-- BEFORE FILING: please run

       zsh-history-enquirer --version
       echo "$ZSH_VERSION $TERM $(uname -srm)"

     and paste the output below. Without this we cannot tell whether
     the bug is in the binary, your zsh build, or the terminal. -->

### What happened

<!-- 1-3 sentences. -->

### Steps to reproduce

1.
2.
3.

### What you expected

### What actually happened

### Environment

- `zsh-history-enquirer --version`:
- `echo "$ZSH_VERSION $TERM $(uname -srm)"`:
- Install method (npm / brew / source):
- Terminal emulator (iTerm2, Alacritty, kitty, …):

### Logs

<!-- If the bug is reproducible, set ZHE_DEBUG and capture:

       ZHE_DEBUG=/tmp/zhe.log zsh -i
       # …reproduce the bug…
       cat /tmp/zhe.log

     Paste the contents below. -->

```
```

### Anything else

<!-- A picture is worth a thousand bytes. asciinema recording > screenshot > nothing. -->
