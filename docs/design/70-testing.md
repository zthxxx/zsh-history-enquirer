# design/70-testing — what runs where

## Test layers

| Layer | Where | What |
| --- | --- | --- |
| **unit** | `internal/**/*_test.go` | Pure Go. No zsh, no docker. Property-based tests via `pgregory.net/rapid` plus targeted table tests. Runs with `task test:unit`. |
| **integration** | same package | Components that need a pty (via `creack/pty`) — keys reader, tty cursor probe. Skipped on Windows. Still no docker. |
| **e2e** | `e2e/{debian,alpine}/` | Real binary inside docker, real zsh, real pty, scripted via `expect`. Two libcs (glibc / musl). **Never** runs against the user's machine. |

## Why e2e in docker

The legacy `tests/zsh-widget.test.zsh` runs an `expect` script
against `zsh -il` on the developer's box. It works, but:

- Risks mutating `~/.zsh_history` if `$HOME` isn't sandboxed.
- Depends on whatever zsh + plugins are installed locally.
- Tickles the developer's terminal emulator with raw escapes —
  bad UX and flaky on certain emulators.

Putting it in docker:

- Pins zsh and OS versions (debian:bookworm-slim and alpine:3.20).
- Runs against a fresh `$HOME` per scenario.
- Exits cleanly even on a CI-killed run because the container is
  torn down.

## Docker images

We run e2e on **two** libc surfaces because users actually run on
both:

- `e2e/debian/Dockerfile` — bookworm-slim, glibc.
- `e2e/alpine/Dockerfile`  — alpine 3.20, musl.

Both install `zsh expect ca-certificates` and create a `tester`
user with `/bin/zsh` as the shell. The entrypoint is `e2e/run.sh`,
which seeds `$HOME` per scenario and runs each `*.exp` file.

The binary (`bin/zsh-history-enquirer-linux-amd64`) is mounted at
`/usr/local/bin/zsh-history-enquirer`. The plugin file is mounted
under `/opt/zsh-history-enquirer/plugin.zsh`. `~/.zshrc` is
generated by `run.sh` and sources the plugin.

## Scenario coverage

`e2e/scenarios/*.exp` (25 scenarios at last count):

| # | Name | Asserts |
| --- | --- | --- |
| 01 | basic-pick | empty input → ^R → echo ok appears, Enter submits |
| 02 | multi-line-scroll | scroll past a 5-line history entry |
| 03 | cancel-preserves-input | Esc returns the typed-input invariant |
| 04 | multi-word-search | AND-filter narrows to entries with both tokens |
| 05 | paste-bracketed | `\e[200~ … \e[201~` reaches Input as one event |
| 06 | pageup-pagedown | rotation by limit |
| 07 | home-end | head + tail focus |
| 08 | prefilter-from-lbuffer | argv input pre-filters before the first keystroke |
| 09 | multiline-submit | select + run a multi-line entry |
| 10 | multiline-render-and-cancel | filter to a multi-line entry, render, cancel |
| 11 | multiline-scroll-into-view | arrow-down a multi-line entry into the visible window |
| 12 | empty-history | "(no matches)" hint when fixture is empty |
| 13 | unicode-entries | CJK / accented / emoji entries render and filter |
| 14 | long-line-wrap | a 200-char entry wraps onto multiple rows correctly |
| 15 | vi-keymap | ^R works in viins and vicmd keymaps (regression test) |
| 16 | narrow-terminal-wrap | 40-col × 24-row terminal forces multi-line wrap |
| 17 | input-edit | Backspace + Ctrl-U re-filter |
| 18 | flag-shaped-lbuffer | LBUFFER beginning with `-` is forwarded as input, not parsed as a flag |
| 19 | ctrl-w-word-delete | Ctrl-W kills the previous word + trailing whitespace |
| 20 | alt-backspace | `\e\x7f` (and `\e\x08`) alias Ctrl-W; Esc-prefix does NOT cancel |
| 21 | input-wrap-edit | input row that wraps to a second visual line still edits cleanly |
| 22 | paste-with-control-bytes | bracketed paste payload containing control bytes lands verbatim |
| 23 | f-key-no-cancel | F1/F2 (`\eOP` / `\eOQ`) are silently swallowed — picker stays open |
| 24 | multiline-narrow-wrap-submit | multi-line entry on a 40-col terminal where each line also wraps; submit preserves every byte |
| 25 | multiline-scroll-down-no-stick | ↓ at the visible bottom advances focus onto a multi-line entry below — no rotate-then-clamp "lost keypress" |

Each scenario is an idempotent expect script with a per-test
outer timeout (12s default; the multi-line and submit-and-run
scenarios bump to 15–25s because each `\r` triggers a real zsh
eval whose round-trip dominates the budget). Failures dump the
transcript to stdout.

## act compatibility

`task ci:e2e:run TARGET=debian` (or `TARGET=alpine`) is the
**single recipe** invoked both by GitHub Actions and by `act`. It
builds the image (cache-keyed by Dockerfile hash) and runs every
scenario. The CI workflow file passes the same `task` command to
the runner; there is no "works locally, fails on CI" gap.

`.actrc` pins `catthehacker/ubuntu:act-latest` because the
official guidance is to use an image that keeps `node` available
for `go-task/setup-task@v2`.

## Property tests (unit-layer)

Used in:

- `internal/history`: reverse + dedupe + unescape invariants.
- `internal/search`: tokenize / AndFilter monotonicity, payload-
  preservation.
- `internal/ui/wrap`: row-count monotonicity in input length.
- `internal/ui/highlight`: payload preservation under SGR
  stripping.
- `internal/keys/parser`: chunk-boundary invariance — splitting
  the same input at any byte boundary yields the same Event
  sequence as feeding it whole.

Test names use the `TestProperty_` prefix so `task test:property`
runs them in isolation.

## Fuzz tests

`internal/keys/parser_test.go` contains
`FuzzParser_NoPanicOnArbitraryBytes` — a Go-native fuzz target
that asserts `Parser.Feed` never panics regardless of input.
Standard `go test` runs each seed once as a regression check;
longer fuzzing runs via `go test -fuzz=... -fuzztime=...`.

## Smoke tests

`task release:smoke` exercises the rendered npm shim end-to-end:

1. `--version` locates the platform binary and execs it.
2. `--print-install-hint` emits the source-line hint.
3. Missing-binary fallback echoes argv back to stdout (the
   widget contract).

The smoke runs `release:dry-run` first so it always tests the
freshly-rendered packages.

## Coverage gate

CI fails if total coverage drops below **70%**. Actual coverage
hovers around 89–90%, so the gate has ~20 points of headroom for
legitimate untestable additions while still flagging
regressions. The pull-up gate applies to `coverage.out` produced
by `task ci:unit` (which is
`go test -race -count=1 -coverprofile=coverage.out ./...`).

Per-package targets:

| Package | Coverage | Notes |
| --- | --- | --- |
| `internal/search` | 100% | tokenize + AND-filter, pure functions. |
| `pkg/version` | 100% | -ldflags-injected fields. |
| `internal/ui` | ~98% | renderer + FSM; only debug-format strings uncovered. |
| `internal/history` | ~95% | ZshLoader's exec wrapper requires zsh. |
| `internal/keys` | ~94% | parser + reader; pty-driven master-close exit pinned, the remaining couple of percent is the EINTR retry path on the live read syscall. |
| `internal/tty` | ~75% | termios + cursor probe; pty-driven Size + Close-while-raw paths now pinned. /dev/tty Open() and NewDevTTY remain uncovered (require a real controlling terminal). |
| `internal/app` | ~74% | Run() body needs a TTY; pty tests cover runEventLoop submit / cancel / preEvents / channel-closed / trailing-flush, fetchInitialState's twin panic-recovery defenses, and readGeometry's three arms. e2e covers the orchestration layer. |
| `cmd/zsh-history-enquirer` | ~45% | main() needs a TTY; the panic-recovery helpers and version/help fast-paths are unit-tested, full main() flow is smoke-tested via the binary. |
